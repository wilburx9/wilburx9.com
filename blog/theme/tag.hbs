{{!< default}}

<div class="gh-page">
    {{> "full-header"}}
    <div class="gh-postfeed">
        {{#tag}}
            {{#get "posts" filter="primary_tag:{{slug}}" include="tags"}}
                {{#foreach posts}}
                    {{> "post-card"}}
                {{/foreach}}
            {{/get}}
        {{/tag}}
    </div>
    {{> "listing-footer"}}
</div>
